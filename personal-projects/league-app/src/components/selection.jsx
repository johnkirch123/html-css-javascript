import React, { Component } from "react";
// Will need to use the api on the back end to get data,
// tie in the api with this file from the backend.
class Selection extends Component {
  state = {};
  render() {
    return <button className="btn btn-primary">Query DB</button>;
  }
}

export default Selection;
